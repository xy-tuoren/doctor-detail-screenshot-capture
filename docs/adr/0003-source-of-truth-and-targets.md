# 补全目标为莲藕系统，数据源以机构端 UI 导出 .xls 为准

补全的写回目标是莲藕系统（通过其修改数据接口）。机构端数据有两条导出路径：UI 自动化导出的 `.xls`（列名固定，含身份证号、审核日期、开始/结束日期）和 `cmd/reg-api` 的 SOAP 导出 `.xlsx`（列名动态）。决定以 **UI 导出的 .xls 为核对与补全的唯一数据源**，因为其列结构与已确认的字段映射规则一一对应、可预期；SOAP 导出（`cmd/reg-api`）不参与本管线，视为遗留能力。核对的匹配键为身份证号，姓名仅作辅助校验。
